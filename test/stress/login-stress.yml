config:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 50
    - duration: 30
      arrivalRate: 100
scenarios:
  - flow:
      - post:
          url: "/api/auth/login"
          json:
            email: "admin@admin.com"
            senha: "123456"
